# 数据库建表文档 / 表结构

> 项目：店牛预警平台（WarningPlatform）
> 数据库：PostgreSQL（Supabase / 自建 Postgres 均可）
> 版本说明：本文档由生产库 `information_schema` + `pg_indexes` 真实导出生成，与线上表结构一致。

## 总览

| # | 表名 | 说明 | 主键 |
|---|------|------|------|
| 1 | `health_check` | 系统健康检查表（禁止删除/修改） | `id` |
| 2 | `data_tables` | 上传的数据表（元数据：字段标签 + 预览行） | `id` |
| 3 | `data_tables_row` | 上传数据表的行级存储（一数据行 = 一条 jsonb 记录，支撑超大表） | `(id, seq)` |
| 4 | `table_groups` | 数据表分组 | `id` |
| 5 | `rule_groups` | 预警规则分组 | `id` |
| 6 | `alert_rules` | 预警规则（含画布 nodes/edges、调度、适用对象、执行记录） | `id` |
| 7 | `alert_tasks` | 预警工单（由规则触发产生的待处理告警，一行一条） | `id` |
| 8 | `dealers` | 经销商字典 | `id` |
| 9 | `stores` | 店仓字典 | `id` |
| 10 | `employees` | 员工字典 | `id` |
| 11 | `organizations` | 组织架构（总部/分公司/部门/区域/门店） | `id` |
| 12 | `persons` | 人员（挂在组织节点下） | `id` |
| 13 | `hr_attributes` | 人事属性字典（职位/岗位/部门/经销商属性/店仓属性） | `id` |
| 14 | `home_config` | 登录页/首页管理配置 | `id` |
| 15 | `sync_lock` | 数据同步分布式锁 | `lock_name` |
| 16 | `sync_tasks` | 数据同步任务 | `id` |
| 17 | `sync_instances` | 数据同步实例 | `id` |
| 18 | `sync_data_sources` | 数据同步数据源 | `id` |
| 19 | `sync_datasets` | 数据同步数据集 | `id` |
| 20 | `sync_channels` | 数据同步通道 | `id` |
| 21 | `sync_meta_cache` | 数据同步元数据缓存 | `id` |
| 22 | `sync_watermark` | 数据同步水位（增量标记） | `id` |
| 23 | `sync_audit` | 数据同步审计日志 | `id` |

所有业务表均遵循「聚合根 + JSONB」持久化模式：前端直接读写整份对象，
结构化列（如 `id/name/status/created_at`）用于索引与检索，业务明细存于 `data`/`attrs`/`config`/`preview` 等 JSONB 字段。
⚠️ 例外：`data_tables` 的全量行数据已拆到 `data_tables_row`（一行一个 jsonb），`data_tables.data` 只保留元数据，以支撑超大表（>20 万行）；其余表整对象仍存于自身 JSONB 字段。
对应前端类型见 `src/lib/types.ts`。

---

## 建表语句（PostgreSQL）

### 1. health_check — 系统健康检查表

```sql
CREATE TABLE IF NOT EXISTS public.health_check (
    id          integer GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    updated_at  timestamptz DEFAULT now(),
    CONSTRAINT health_check_pkey PRIMARY KEY (id)
);
```

### 2. data_tables — 上传的数据表（元数据）

> 仅存元数据（字段标签 + 预览行 + 行数）；全量行数据拆到 `data_tables_row` 行级表。

```sql
CREATE TABLE IF NOT EXISTS public.data_tables (
    id         varchar(64) PRIMARY KEY,
    name       varchar(255) NOT NULL,
    file_name  varchar(255) NOT NULL DEFAULT '',
    row_count  bigint NOT NULL DEFAULT 0,
    created_at bigint NOT NULL,
    data       jsonb NOT NULL,   -- DataTable（不含 rows），含 fields/previewRows/group/prev 等
    updated_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS data_tables_data_gin   ON public.data_tables USING gin (data);
CREATE INDEX IF NOT EXISTS data_tables_created_idx ON public.data_tables USING btree (created_at);
```

### 3. data_tables_row — 上传数据表的行级存储

> 一数据行 = 一条记录（单个 jsonb），用于支撑超大表（>20 万行），
> 避免把整表塞进 `data_tables.data` 单个 jsonb 超限。`seq` 为该行在表内的 0 起始序号。

```sql
CREATE TABLE IF NOT EXISTS public.data_tables_row (
    id   varchar(64) NOT NULL,   -- 归属 data_tables.id
    seq  bigint     NOT NULL,    -- 行序号（0 起始，唯一）
    data jsonb      NOT NULL     -- 单行数据（键为列名）
);
ALTER TABLE ONLY public.data_tables_row ADD CONSTRAINT data_tables_row_pkey PRIMARY KEY (id, seq);
CREATE INDEX IF NOT EXISTS data_tables_row_id_gin ON public.data_tables_row USING gin (data);
CREATE INDEX IF NOT EXISTS data_tables_row_id_idx ON public.data_tables_row USING btree (id, seq);
```

### 4. table_groups — 数据表分组

```sql
CREATE TABLE IF NOT EXISTS public.table_groups (
    id         text PRIMARY KEY,
    name       text NOT NULL,
    sort       bigint NOT NULL DEFAULT 0,
    created_at bigint NOT NULL DEFAULT 0
);
```

### 5. rule_groups — 预警规则分组

```sql
CREATE TABLE IF NOT EXISTS public.rule_groups (
    id         text PRIMARY KEY,
    name       text NOT NULL,
    created_at bigint NOT NULL
);
```

### 6. alert_rules — 预警规则

```sql
CREATE TABLE IF NOT EXISTS public.alert_rules (
    id            varchar(64) PRIMARY KEY,
    name          varchar(255) NOT NULL,
    status        varchar(32)  NOT NULL DEFAULT 'draft',  -- draft|active|paused
    created_at    bigint NOT NULL,
    updated_at_ms bigint NOT NULL,
    data          jsonb NOT NULL  -- 画布 nodes/edges、调度、适用对象、执行记录
);
CREATE INDEX IF NOT EXISTS alert_rules_status_idx ON public.alert_rules USING btree (status);
CREATE INDEX IF NOT EXISTS alert_rules_data_gin   ON public.alert_rules USING gin (data);
```

### 7. alert_tasks — 预警工单

```sql
CREATE TABLE IF NOT EXISTS public.alert_tasks (
    id            varchar(64) PRIMARY KEY,
    rule_id       varchar(64)  NOT NULL DEFAULT '',
    rule_name     varchar(255) NOT NULL DEFAULT '',
    level         varchar(32)  NOT NULL DEFAULT 'warn',
    title         varchar(255) NOT NULL DEFAULT '',
    content       text NOT NULL DEFAULT '',
    reason        text,
    condition_desc text,
    preview       jsonb,                -- 预警快照（店仓/接收人等）
    dept          varchar(255) NOT NULL DEFAULT '',
    assignee      varchar(255) NOT NULL DEFAULT '',
    status        varchar(32)  NOT NULL DEFAULT 'new',  -- new|processing|done|failed
    handoff_to    varchar(255),
    created_at    bigint NOT NULL,
    updated_at    bigint NOT NULL,
    -- 处理流程字段
    accepted_at   bigint,
    started_at    bigint,
    handled_at    bigint,
    resolution    text,                 -- 完成时处理方案
    failed_reason text,                 -- 无法完成时原因
    comments      jsonb,                -- 留言/回复
    plan          text                  -- 预警处理方式草稿
);
CREATE INDEX IF NOT EXISTS alert_tasks_status_idx ON public.alert_tasks USING btree (status);
CREATE INDEX IF NOT EXISTS alert_tasks_rule_idx   ON public.alert_tasks USING btree (rule_id);
```

### 8. dealers — 经销商

```sql
CREATE TABLE IF NOT EXISTS public.dealers (
    id         text PRIMARY KEY,
    name       text NOT NULL,
    sort       bigint NOT NULL DEFAULT 0,
    created_at bigint NOT NULL DEFAULT 0,
    code       text,          -- 经销商编号（账号）
    contact    text,
    phone      text,
    address    text,
    password   text,
    birthday   text,
    enabled    boolean NOT NULL DEFAULT true,
    attrs      jsonb,         -- 字典分类属性标签（经销商等级/分类等）
    province   text,
    city       text,
    district   text
);
```

### 9. stores — 店仓

```sql
CREATE TABLE IF NOT EXISTS public.stores (
    id          text PRIMARY KEY,
    name        text NOT NULL,
    sort        bigint NOT NULL DEFAULT 0,
    created_at  bigint NOT NULL DEFAULT 0,
    code        text,          -- 店仓编号（账号）
    contact     text,
    phone       text,
    address     text,
    password    text,
    birthday    text,
    enabled     boolean NOT NULL DEFAULT true,
    attrs       jsonb,         -- 主营品牌/分公司/部门/销售区域/区部 等属性
    dealer_id   text,          -- 上级经销商
    brand       text,
    company     text,
    department  text,
    sales_area  text,
    district    text,
    allow_retail boolean       -- 是否允许零售
);
```

### 10. employees — 员工

```sql
CREATE TABLE IF NOT EXISTS public.employees (
    id         text PRIMARY KEY,
    code       text,          -- 员工编号（账号）
    name       text NOT NULL,
    dealer_id  text,
    store_id   text,
    post       text,
    on_duty    boolean DEFAULT true,
    enabled    boolean DEFAULT true,
    sort       integer DEFAULT 0,
    created_at bigint DEFAULT 0,
    password   text,
    attrs      jsonb
);
```

### 11. organizations — 组织架构

```sql
CREATE TABLE IF NOT EXISTS public.organizations (
    id         text PRIMARY KEY,
    name       text NOT NULL,
    kind       text NOT NULL DEFAULT '其他',   -- 总部/分公司/部门/区域/门店/其他
    parent_id  text,                           -- 上级组织（平级标签，默认不同路）
    sort       bigint NOT NULL DEFAULT 0,
    created_at bigint NOT NULL
);
```

### 12. persons — 人员

```sql
CREATE TABLE IF NOT EXISTS public.persons (
    id            text PRIMARY KEY,
    name          text NOT NULL,
    org_id        text NOT NULL DEFAULT '',
    title         text,                -- 职位
    supervisor_id text,                -- 上级人员
    manage_scope  jsonb,               -- 管理范围：{tableId, filters:[{attrName,values}], storeIds}
    phone         text,
    email         text,
    enabled       boolean NOT NULL DEFAULT true,
    sort          bigint NOT NULL DEFAULT 0,
    created_at    bigint NOT NULL,
    username      text,                -- 登录账号
    id_card       text,
    address       text,
    birthday      text,
    password      text,
    post          text,                -- 岗位（RBAC 角色键）
    dealer_id     text,                -- 归属经销商
    store_id      text                 -- 归属店仓
);
```

### 13. hr_attributes — 人事属性字典

```sql
CREATE TABLE IF NOT EXISTS public.hr_attributes (
    id         text PRIMARY KEY,
    name       text NOT NULL,
    items      jsonb NOT NULL DEFAULT '[]'::jsonb,   -- {id,name}[] 标签条目
    sort       bigint NOT NULL DEFAULT 0,
    created_at bigint NOT NULL,
    category   text NOT NULL DEFAULT 'person'          -- person|dealer|store
);
```

### 14. home_config — 登录页/首页管理配置

```sql
CREATE TABLE IF NOT EXISTS public.home_config (
    id         text PRIMARY KEY,     -- 'home'
    config     jsonb NOT NULL,       -- HomeConfig（含页面/登录框样式 + permissions/permOverrides 附加键）
    updated_at bigint NOT NULL DEFAULT 0
);
```

### 15. sync_lock — 数据同步分布式锁

```sql
CREATE TABLE IF NOT EXISTS public.sync_lock (
    lock_name  varchar(128) PRIMARY KEY,
    holder     text,
    expires_at timestamptz
);
```

### 16-23. 数据同步元数据表（统一「id + data jsonb + updated_at」结构）

```sql
CREATE TABLE IF NOT EXISTS public.sync_tasks       (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_instances   (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_data_sources(id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_datasets    (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_channels    (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_meta_cache  (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_watermark   (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS public.sync_audit       (id varchar(64) PRIMARY KEY, data jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now());
```

---

## 权限与数据权限说明

- **RBAC 权限**：`RolePerm.pages`（`Partial<Record<PermModule, PagePerm>>`）与 `dataScope` 一并存于 `home_config.config.permissions`、覆盖存于 `config.permOverrides`（JSONB，无需独立表）。
- **数据权限**：`Person.manage_scope` 存 `{tableId, filters:[{attrName, values}], storeIds}`，表示"管理范围＝某数据表某字段=某值"筛选出的门店集合；解析逻辑见 `src/lib/perm.ts::manageStoreIds`。
- **登录账号**：`persons.username`（管理员）/ `persons` 也可用 `name`；经销商用 `dealers.code`、店仓用 `stores.code`、员工用 `employees.code`，默认密码 `123456`。